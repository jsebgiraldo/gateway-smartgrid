# Secrets — never commit these (only *.env.example templates allowed)
*.env
!*.env.example
# Credentials reference docs live in secrets/ and must never be committed
secrets/*.md

# MCP audit logs
logs/
*.log

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
*.swp
backups/
